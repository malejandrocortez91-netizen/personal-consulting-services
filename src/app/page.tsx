// This file is no longer used, the root page is now src/app/[locale]/page.tsx
// It can be deleted, but we are keeping it to avoid breaking the build.
// Next.js requires a root page.tsx or layout.tsx file.
// We are redirecting to the default locale in the middleware.
export default function RootPage() {
  return null;
}
